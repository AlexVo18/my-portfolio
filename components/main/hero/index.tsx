import ContainerWrapper from "@/components/layout/container";

const HeroSection = () => {
  return (
    <ContainerWrapper>
      <div>HeroSection</div>
    </ContainerWrapper>
  );
};

export default HeroSection;
